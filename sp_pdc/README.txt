Projects from the SP PDC course. Created a website for movie reviews.

React (web), React Native (app), NodeJS (backend).
Backend is running on an AWS EC2 instance, configured with pm2 to boot when the VM spins. 
Also uses S3 for storage and RDS for a MySQL database.
